namespace Engrana.Domain;

public enum FieldType
{
    Boolean, //"true" or "false"
    Input, //user input
    SingleFromSet, //User selects 1 from set
    MultiFromSet, //User selects multiple from set
}
